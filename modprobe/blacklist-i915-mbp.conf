# Restore the original MacBookPro11,3 display path: EFI simpledrm only.
# Kernel 7.0 enumerates the Intel iGPU; X then opens a second modeset device
# and DPMS/unblank fights the panel blank (desktop keeps coming back).
blacklist i915
