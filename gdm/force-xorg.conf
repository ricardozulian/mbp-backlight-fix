# Fragment for /etc/gdm3/custom.conf [daemon]
# The GDM greeter is mutter-on-simpledrm Wayland; that path never sets DRM
# DPMS, so gmux PWM stays on. Force an Xorg greeter so DPMS works.
WaylandEnable=false
